# حماية الموقع من الوصول العام
# إعادة توجيه جميع الزوار إلى صفحة الصيانة

# منع الوصول إلى الملفات الحساسة
<Files "*.yml">
    Order Allow,Deny
    Deny from all
</Files>

<Files "*.md">
    Order Allow,Deny
    Deny from all
</Files>

# إعادة توجيه إلى صفحة الصيانة
RewriteEngine On
RewriteCond %{REQUEST_URI} !^/maintenance\.html$
RewriteCond %{REQUEST_URI} !^/robots\.txt$
RewriteCond %{REQUEST_URI} !^/\.htaccess$
RewriteRule ^(.*)$ /maintenance.html [R=302,L]

# منع فهرسة الموقع
Header set X-Robots-Tag "noindex, nofollow, nosnippet, noarchive"
